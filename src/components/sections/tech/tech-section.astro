---
import WalkingLogo from './components/walking-logo.svelte';

const { className } = Astro.props;
---
<div class={`flex flex-col gap-10 ${className}`}>
    <WalkingLogo />
    <div class="flex flex-col items-center text-wri-grey-600">
        <p>We use insdustry-standard tools to explore the latest tech</p>
        <p>and design innovations together as a community.</p>
    </div>
</div>
